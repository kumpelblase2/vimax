import metadata from './metadata';
import library from './library';
import plugin from './plugin';

export default {
    namespaced: true,
    modules: {
        metadata,
        library,
        plugin
    }
}
